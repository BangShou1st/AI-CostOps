# M9 Human Gates

Human input is required only where evidence cannot be generated safely by automation alone:

```text
AIC-082: provide/select one real Provider export locally and review redacted certification report
AIC-083: final release acceptance/sign-off
```

Implementation agents must not request or copy real Provider secrets into GitHub issues, PRs, logs, fixtures, or committed docs.
