package com.aicostops.cost.domain;

/** Attribution hint kinds persisted in {@code attribution_hint}. */
public enum HintType {
    PROVIDER_API_KEY,
    PROVIDER_PROJECT,
    PROVIDER_USER,
    EMPLOYEE_SELECTION
}
