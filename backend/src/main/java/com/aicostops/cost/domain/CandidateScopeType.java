package com.aicostops.cost.domain;

/** Internal scope kinds an attribution hint could resolve to (AIC-035+; unused by Group 1). */
public enum CandidateScopeType {
    PROJECT,
    COST_CENTER,
    TEAM
}
